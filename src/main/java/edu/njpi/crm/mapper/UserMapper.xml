<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.njpi.crm.mapper.UserMapper">
    <select id="findUser" parameterType="String" resultMap="IdUser" resultType="edu.njpi.crm.model.User">
            select * from tb_user where user_code=#{userCode}
                                        and  user_password=#{passwd}
                                        and user_state='1'
    </select>
    <resultMap id="IdUser" type="edu.njpi.crm.model.User">
        <id property="userId" column="user_id"/>
        <result property="userCode" column="user_code"/>
        <result property="userName" column="user_name"/>
        <result property="password" column="user_password"/>
        <result property="state" column="user_state"/>
    </resultMap>
</mapper>